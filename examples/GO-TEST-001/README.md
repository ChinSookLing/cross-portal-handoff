# GO-TEST-001

Status when fetched: finished. Result field: Sol resigns. White wins.

See ../README.md for what these files are.

- `reply-001-E5.txt` — move 1, recorded player `gpt`, coord `E5`, source `inferred`, coord_source `inferred`, raw_fidelity `unverified`, portal `None`, session_id `None`
- `reply-008-D3.txt` — move 8, recorded player `claude`, coord `D3`, source `inferred`, coord_source `inferred`, raw_fidelity `unverified`, portal `None`, session_id `None`
- `reply-071-resign.txt` — move 71, recorded player `gpt`, coord `resign`, source `courier`, coord_source `courier`, raw_fidelity `verified`, portal `None`, session_id `None`
- `handoff-closed.txt` — table handoff for the finished game, fetched 2026-09-25
